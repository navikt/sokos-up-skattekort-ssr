apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: sokos-up-skattekort-ssr
  namespace: okonomi
  labels:
    team: okonomi
spec:
  image: "{{{ image }}}"
  port: 3000
  liveness:
    path: /api/internal/isAlive
    initialDelay: 10
  readiness:
    path: /api/internal/isReady
    initialDelay: 10
  replicas:
    min: 1
    max: 2
  observability:
    autoInstrumentation:
      enabled: true
      runtime: nodejs
  azure:
    application:
      enabled: true
      allowAllUsers: false
      claims:
        groups:
          - id: "62e34335-63b3-420a-8978-62d48d218c19" # 0000-CA-SOKOS-MF-Skattekort-ssr
        extra:
          - NAVident
  accessPolicy:
    inbound:
      rules:
        - application: sokos-utbetalingsportalen
    outbound:
      rules:
        - application: sokos-skattekort
  webproxy: true
  resources:
    limits:
      memory: 1024Mi
    requests:
      cpu: 50m
      memory: 128Mi
  env:
    - name: SOKOS_SKATTEKORT_API
      value: https://sokos-skattekort:8080
    - name: SOKOS_SKATTEKORT_API_AUDIENCE
      value: "api://dev-gcp.okonomi.sokos-skattekort/.default"
